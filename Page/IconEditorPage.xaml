<?xml version="1.0" encoding="utf-8"?>

<Page NavigationCacheMode="Enabled"
      x:Class="Foldicon.Page.IconEditor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:userControl="using:Foldicon.UserControl"
      xmlns:class="using:Foldicon.Class"
      mc:Ignorable="d">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Top" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>


            <TextBox
                IsReadOnly="True"
                Text="{x:Bind ParentFolder, Mode=OneWay}"
                Grid.Column="0"
                PlaceholderText="请选择文件夹路径..." />
            <Button
                Click="PickFolderButton_Click"
                Grid.Column="1"
                Content="选择文件夹"
                Margin="12,0,0,0" />
     
            <Button
                Style="{ThemeResource AccentButtonStyle}"
                Click="ApplyAllButton_Click"
                Grid.Column="2"
                Content="应用所有图标"
                Margin="6,0,0,0"
                IsEnabled="{x:Bind IsSubFoldersLoaded, Mode=OneWay}"/>

        </Grid>

        <Grid VerticalAlignment="Top" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <AutoSuggestBox PlaceholderText="搜索文件夹名称..."
                            Grid.Column="0"
                            Text="{x:Bind FolderNameFilter, Mode=TwoWay}"
                            TextChanged="FolderNameFilter_TextChanged"
                            QueryIcon="Find"
                            IsEnabled="{x:Bind IsSubFoldersLoaded, Mode=OneWay}"/>

            <SplitButton Grid.Column="1"
                         Padding="0"
                         Margin="12,0,0,0">
                <ToggleButton Content="启用筛选" 
                              IsEnabled="{x:Bind IsSubFoldersLoaded, Mode=OneWay}"/>
                <SplitButton.Flyout>
                    <MenuFlyout>
                        <RadioMenuFlyoutItem Text="全部" GroupName="TypeGroup" />
                        <RadioMenuFlyoutItem Text="系统默认" GroupName="TypeGroup" />
                        <RadioMenuFlyoutItem Text="自定义图标" GroupName="TypeGroup" />
                        <MenuFlyoutSeparator />
                        <RadioMenuFlyoutItem Text="全部" GroupName="StateGroup" />
                        <RadioMenuFlyoutItem Text="未修改项" GroupName="StateGroup" />
                        <RadioMenuFlyoutItem Text="待应用项" GroupName="StateGroup" />
                    </MenuFlyout>
                </SplitButton.Flyout>
            </SplitButton>
        </Grid>

        <Line Grid.Row="2"
              Stroke="{ThemeResource AccentAAFillColorDisabled}"
              X1="0" Y1="0"
              X2="999999" Y2="0"
              StrokeThickness="1" />

        <Grid Grid.Row="3">
            <ScrollViewer>
                <ItemsRepeater ItemsSource="{x:Bind FilteredSubFolders, Mode=OneWay}">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="class:FolderEntry">
                            <userControl:FolderEntryControl FolderEntry="{x:Bind}" />
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="8" />
                    </ItemsRepeater.Layout>
                </ItemsRepeater>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>